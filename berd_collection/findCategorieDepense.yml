info:
  name: findCategorieDepense
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/depenses/stats/barres?periode=journaliere
  params:
    - name: periode
      value: journaliere
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
